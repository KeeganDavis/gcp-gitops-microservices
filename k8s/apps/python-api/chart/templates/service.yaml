apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-python-api-service
spec:
  selector:
    app: python-api
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}